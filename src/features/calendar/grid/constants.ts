/** Default vertical scale: 2px per minute → 30min slot = 60px */
export const DEFAULT_PX_PER_MINUTE = 2;

/** Padding above/below shop hours on the grid (minutes). */
export const GRID_PADDING_MIN = 60;
