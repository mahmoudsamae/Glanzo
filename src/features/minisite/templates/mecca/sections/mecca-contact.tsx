import type { ShopPublicData } from "@/lib/validations/public-shop";

type MeccaContactSectionProps = {
  data: ShopPublicData;
  shopSlug: string;
  preview?: boolean;
};

/** Contact section — full implementation in a follow-up prompt. */
export function MeccaContactSection(_props: MeccaContactSectionProps) {
  return null;
}
