import type { ShopPublicData } from "@/lib/validations/public-shop";

type MeccaGallerySectionProps = {
  data: ShopPublicData;
  preview?: boolean;
};

/** Gallery section — full implementation in a follow-up prompt. */
export function MeccaGallerySection(_props: MeccaGallerySectionProps) {
  return null;
}
